/**
 * Local toast types (replaces @kitman/components/src/types and Toast types)
 */
export const TOAST_TYPE = { MESSAGE: 'MESSAGE', DEFAULT: 'DEFAULT' };

export const toastRemovalDelayEnumLike = {
  DefaultRemovalDelay: 5000,
  ShortRemovalDelay: 3000,
  LongRemovalDelay: 8000,
};
