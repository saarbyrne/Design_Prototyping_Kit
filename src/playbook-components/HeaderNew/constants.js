// @flow

// Maximum characters for title display on mobile before truncation
export const MAX_TITLE_CHARS_MOBILE: number = 30;
export const MAX_LIST_ITEM_CHARS: number = 30;
// Maximum visible badges before showing overflow
export const MAX_VISIBLE_BADGES_MOBILE: number = 2;
export const MAX_VISIBLE_BADGES_DESKTOP: number = 5;

// Avatar dimensions
export const AVATAR_SIZE: number = 70;

// Menu positioning
export const MENU_ANCHOR_ORIGIN = {
  vertical: 'bottom',
  horizontal: 'right',
};

export const MENU_TRANSFORM_ORIGIN = {
  vertical: 'top',
  horizontal: 'right',
};